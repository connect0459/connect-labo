[project]
name = "sync-cmd"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sync-cmd = "sync_cmd.main:main"

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
